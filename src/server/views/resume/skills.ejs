<% if (resume.specialties.length > 0) { %>
<div class="resume-skills">

    <h2>Skills</h2>

    <ul>
    <% resume.skills.forEach(function(skill) { %>
        <li>
            <%= skill %>
        </li>
    <% }); %>
    </ul>
</div>
<% } %>
